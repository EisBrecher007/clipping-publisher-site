name = "clipping-publisher-tiktok-auth"
main = "src/worker.js"
compatibility_date = "2026-09-20"

# Create the namespace before deployment, then replace the placeholder. Never put secrets here.
[[kv_namespaces]]
binding = "TOKENS"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
